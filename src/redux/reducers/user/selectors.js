export const getUserIsLoggedIn = (state) => state.user.isLoggedIn;

export const getUserHasAuthEnded = (state) => state.user.hasAuthEnded;

export const getUserUsername = (state) => state.user.username;

export const getUserEmail = (state) => state.user.email;
